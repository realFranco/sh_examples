# MacBook

```bash
# Some instructions to execute in order to have a fully working software development environment in apple environments.

# Vscode, add the `code` command into the path:
# control + p
# > shell command: Install code command in PATH.
# See: https://stackoverflow.com/a/76889208/10178933.

```
